# Nur die eigenen Tests einsammeln. Ohne diese Begrenzung zieht pytest den
# untracked freqtrade/-Checkout mit ein und crasht beim Kollektieren
# (fehlende Dev-Dependencies wie xdist).
[pytest]
testpaths = tests
